import { EmptyPage } from "@/modules/shared/empty-page";

export default function Page() {
  return (
    <EmptyPage
      title="Flags"
      description="Admin surface for managing flags."
      phase="Phase 1 → 9 (progressively enriched)"
    />
  );
}
